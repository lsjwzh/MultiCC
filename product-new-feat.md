新需求：
帮我把代码里面涉及到api的限额，资源之类的地方，都理出来，做一个上线前资源检查表，避免因为资源不够导致瓶颈


把webcc项目里面的STT去掉，改成别的方式；
把面板那里，使用ai文本后，直接帮用户执行回车

增加功能把webcc公开到外部服务。。。默认使用tailscale操作。配置的端口等配置在manage里面实现。。。


整理一下downloads目录的文件。。。。原有的目录就不用动了，主要把单独的文件进行整理分类；