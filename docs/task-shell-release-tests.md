# 任务壳按需上下文：发布测试与对比方案

日期：2026-09-08。本文是“默认续接最后任务、按需回补壳上下文、事后安全归集”的测试与发布契约。功能设计见 [任务会话壳方案](task-session-shell-experiment.md)。

## 行为不变量

- 普通消息只由服务端 `currentTaskId` 路由；URL、浏览器缓存和客户端 taskId 不能改写目标。
- 当前任务占用时进入同一任务队列，不自动创建任务或工作区。
- 会话内「新任务」创建独立原生上下文，共用壳工作区并串行执行；任务板手动 Fork 才创建新的独立壳和 worktree。
- 每轮默认只使用当前任务原生上下文；其他关联任务的完成历史只可由只读 MCP `get_task_context` 有界回补。
- 回补内容保留 taskId、消息来源和完整回合边界；历史内容只作为资料，不能作为新指令执行。
- 普通会话允许 Aux 事后归集，历史及工具操作仍保留原执行位置；独立任务锁定任务身份。较早回合不能覆盖较新的游标。
- token 提示是省略上下文的估算值；一旦本轮调用回补 MCP，显示节省为 0，并保留原估算值供解释和审计。

## 自动化测试矩阵

测试使用临时目录、隔离 SQLite、受控执行器和假 CLI；并发测试不得共享生产数据。

| 编号 | 场景 | 必须满足 |
| --- | --- | --- |
| R01 | 空闲任务普通发送 | 保持 taskId / sessionId / worktree，使用服务端当前任务 |
| R02 | 当前任务占用时再次发送 | 同 taskId 排队，不创建新执行会话或 worktree |
| R03 | 其他壳关联同一任务 | 仅允许历史引用；跨所有者写入拒绝，不能抢占任务 |
| R04 | 会话内显式新任务 | 新 taskId / 原生 session，共用所属壳 worktree；不隐式继承其他任务历史 |
| R05 | 响应丢失、重启、并发重试 | 同一消息键、目标和下游幂等键；改内容复用键返回冲突 |
| R06 | 受信投递携带 taskId 和任务正文 | 自动定位已有任务；未找到时用原 taskId 和正文创建，锁定归属且不要求人工选择 |
| R07 | 从任务板打开 `chat.html?task=<id>` | 会话来源任务只读，不改变游标或创建执行；独立任务允许执行 |
| C01 | 默认上下文 | prompt 不含其他任务历史，UI 显示正的预计节省值 |
| C02 | 调用 `get_task_context` | 只返回同壳其他任务的有界完成历史；UI 节省归零并显示原估算 |
| C03 | 重复调用回补 | 返回相同内容，不重复写历史、不产生执行副作用，指标保持归零 |
| C04 | 来源仍在输出或历史过长 | 排除 active / partial / cancelled / error 回合；总输出不超过约 60 KB |
| A01 | 只读回合判为已有任务 | 将本轮交接快照挂到目标任务并更新 `currentTaskId` |
| A02 | 只读回合判为新任务 | 创建待启动任务身份，下一条消息才创建原生上下文，仍在同壳工作区执行 |
| A03 | 回合含写入型或未知工具 | 归属可变但操作位置不变，保存带状态的交接证据，不搬动文件或原生历史 |
| A04 | Aux 结果延迟或锚点已变化 | 结果仅审计，不能覆盖更新的任务指针 |
| I01 | 回答 / 调整 / 取消 | 固定原 taskId + turnId / requestId；过期控制明确冲突 |
| I02 | 跨壳同时回答同一问题 | 仅一个答案获得预留，其他答案冲突 |
| S01 | 跨项目、伪造 taskId、畸形 MCP 参数 | 拒绝且不泄露其他壳历史、不启动执行 |
| F01 | 工作区创建或任务索引失败 | receipt 保留脱敏错误和原目标；同键重试不换任务 |
| U01 | 桌面和 390 px 移动页面 | 新任务入口、队列状态、token 提示、失败重试和安全文本渲染可用 |
| E01 | 独立 server + SQLite + scheduler + Git + 假 CLI | HTTP 到正式历史的完整链路通过，工作区与原生会话身份隔离 |

## 离线 A/B/C 对比

比较单位是“同一个脱敏对话在同一目标轮次的重放”，不能用不同问题直接比较 token 或正确率。

| 组别 | 输入策略 | 用途 |
| --- | --- | --- |
| A：全量基线 | 当前任务上下文 + 壳内其他任务全部可用完成历史，每轮预注入 | 对照旧的高成本做法 |
| B：按需方案 | 仅当前任务上下文；模型判断必要时调用 `get_task_context` | 候选发布方案 |
| C：无回补消融 | 仅当前任务上下文，禁用回补工具 | 衡量 MCP 对质量的净贡献，不进入生产 |

语料至少覆盖：直接续作、代词指代、跨任务约束、相似任务干扰、明确新主题、需要旧结论但不需要旧过程、错误或冲突历史、写代码/提交/部署等副作用任务。优先使用脱敏真实样本，再补合成边界样本；同一 case 在三组使用相同模型、provider、effort、系统提示、工具状态和代码基线，并随机执行顺序。非确定模型每 case 至少重放 3 次。

记录以下原始事实，不只记录总分：provider 实际 input/output/cache tokens、回补调用次数和时点、首 token 与总延迟、最终 taskId、是否新建任务、工具调用与文件差异、答案中的事实断言。UI 估算值单独记录，不能冒充 provider 计费数据。

人工评分应隐藏组别，按预先写好的 case rubric 判断：任务完成、旧约束召回、无关上下文污染、是否应回补、任务归集、工作区一致性。自动判分只用于稳定事实（taskId、工具、文件、token、延迟），开放式答案至少双人或“规则 + 模型裁判 + 抽样复核”。

建议发布门槛：B 相对 A 的任务完成率非劣界不低于 -3 个百分点；需要旧上下文的漏回补率不高于 2%；不需要时的误回补率不高于 10%；任务归集 macro-F1 不低于 0.95；错误工作区写入、重复执行和跨项目泄露必须为 0；实际输入 token 中位数至少下降 20%。这些是上线门槛，不是当前自动化测试已经证明的结论。

## 在线灰度

按稳定的 shellId 哈希分桶，避免同一用户跨组漂移。先内部 dogfood，再按 5% → 25% → 100% 放量；每阶段至少覆盖一个完整工作周期。线上只比较 A 与 B，不启用 C。

立即回滚条件：任何跨项目上下文泄露、错误工作区写入、重复副作用、任务指针不可恢复；或 B 的上下文相关失败率连续两个观察窗高于 A 3 个百分点。性能类回滚条件为 p95 总延迟增加超过 20%，或 token 中位数未下降且误回补率超过 10%。回滚只切换新回合的上下文策略，不迁移或删除已有任务、receipt 和历史。

## 验证记录

| 命令 / 环境 | 结果 |
| --- | --- |
| `node --test tests/test-task-shells.js tests/test-task-shell-http.js` | 22 个用例通过；覆盖排队、显式新建、幂等、回补、token 指标和控制消息 |
| `node --test tests/test-router-tool-runtime.js tests/test-router-mcp.js` | MCP 工具声明、调用者 capability 作用域、零参数约束和只读标注通过 |
| `node --test tests/test-classify-live-runner.js` | 只读归集、写入后禁止迁移、延迟 Aux 保护通过 |
| `node --test tests/test-task-shell-cdp.js` | 真实 Chrome 页面通过；当前任务续接、新任务、token 提示、响应丢失、安全渲染和移动布局通过 |
| `npm run test:task-shells:isolated` | 独立 server / SQLite / scheduler / Git worktree / 假 Codex 全链路通过 |
| `npm test` | 完整测试通过，退出码 0 |
| `git diff --check`、i18n 与源码行数治理 | 通过 |

当前验证证明确定性协议和界面行为，不证明真实模型质量、真实计费节省或长期稳定性。真实 Claude / Codex 的效果必须按上述 A/B 方案获取数据后再下结论。

## 发布前必须执行：全新 Docker 安装

`npm run test:release:clean-install`

新增源码文件先 `git add`，命令导出候选文件和 SHA-256 摘要，再构建仅含源码和系统前置依赖的镜像。每次创建无数据卷、无宿主挂载的新容器；开始时要求安装目录、运行数据、node_modules 和 npm 缓存不存在。

容器内以候选源码建立临时 Git 镜像，让真实 `install.sh --branch v<版本> --dir <空目录> --no-service` 执行克隆、npm 安装、原生依赖检查及 .env 创建。安装源使用当前候选，避免测试到 GitHub 上的旧版本；不会验证 GitHub CDN 的可用性。系统前置依赖预装，真实模型 CLI 使用受控替身，不测试厂商登录或模型质量。

随后验证安装生成的 .env、首次启动、Web 资源、建项目/会话、重启恢复，以及以下完整任务链路。失败返回非零，容器退出即删除。Android 与桌面发布工作流都依赖该门槛，成功后才进入构建/发布；CI 保存日志 14 天。

| 编号 | 操作序列 | 断言与自动用例 |
| --- | --- | --- |
| SW01 | A → 新任务 B → 选回 A → 选回 B → 重启续作 | taskId/原生身份稳定；游标指向 B；切换不额外建会话。`test-task-shell-transitions.js` |
| SW02 | 当前 B，从任务板查看旧任务 A | A 只读；B 游标与执行不变，无新 receipt。`test-task-shell-transitions.js` |
| SW03 | 旧请求投递/分类尚未返回，切换到新任务 | 迟到结果不能覆盖新游标；旧客户端明确 taskId/版本被拒绝。`test-task-shell-ownership.js` |
| SW04 | A 执行中，在同壳创建下一任务 | 共用同一真实目录；下一执行等 A 释放；两个独立壳可并行。`test-task-shell-context-isolation.js`、`test-task-shell-isolated.js` |
| FK01 | 当前 B，手动 Fork 旧任务 A；原 A 与副本分别继续 | 新任务/壳/会话；原 A 仍属原壳；后续历史不写入冻结快照。`test-task-shell-transitions.js` |
| FK02 | 来源分支有已提交但未合入 main 的文件，再 Fork | 副本 HEAD 等于来源提交且包含该文件；新目录和原生身份；创建不调用模型。`test-task-shell-isolated.js` |
| FK03 | 双击/响应丢失/重启重试/创建失败后重试 | 稳定键只对应一个副本；失败保留快照及目标。`test-task-shell-ownership.js` |
| FK04 | 来源未提交或同工作区仍执行 | 明确拒绝，不丢代码、不创建半份副本。`test-task-shell-ownership.js` |
| FK05 | 原任务发送/回答/调整/取消；Fork 后发送 | 原任务在任务板均拒绝；副本允许执行并锁定自身任务身份。HTTP 与 Chromium 用例 |
| UI01 | 首次加载失败 → 恢复 → 点击 Fork → 发送 | 失败时不显示输入；显式 Fork 后才可输入；来源游标不变；390px 布局通过。`test-task-shell-cdp.js` |

本节是当前发布门槛；上方历史验证记录仅对应当时的提交，不代表新候选自动通过。

### 2026-09-08 全新安装实测记录

- `npm run test:release:clean-install`：退出码 0；Linux arm64 / Node v22.23.2 / Chromium。
- 166 项容器内单元、HTTP、调度与浏览器用例通过；另通过真实 `install.sh` 安装、首次启动、Web 资源、项目/会话创建、完整聊天页面、MCP 上下文回补及多次重启恢复。
- 使用模拟 CLI，不包含真实厂商登录、模型质量或 App 原生设备验证。
- 测试候选基于 `6058f4798140070fddc5f6004cc836ebcccbb065`，包含本次待提交源码；构建内容摘要 `df50cc6c7c1bec081072c796dc9cb8802e83055b6ae94645b0f9059efbfe5638`。
- 此流程发现并修复了安装脚本未定义 `$NODE` 导致末尾失败，以及测试夹具可执行权限缺失和 Linux Chromium 退出后临时目录清理竞态。成功记录对应修复后的完整新容器复验。
