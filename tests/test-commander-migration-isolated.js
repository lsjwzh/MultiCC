'use strict';
// Compatibility command for the former commander-seeding gate. The production
// upgrade now adopts tasks and must never seed fixed role sessions.
require('./test-task-first-isolated');
