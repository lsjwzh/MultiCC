<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened Runtime is required for notarization, and it denies by default
       several things Electron and Node need. Each entry below is the minimum
       for one of them; none of them are optional for this app.

       JIT and unsigned executable memory: V8 compiles JavaScript at runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Electron's own helper processes are launched with DYLD_* set. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Native Node modules (better-sqlite3 and friends) are not signed with this
       app's Team ID, and library validation would refuse to load them. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- MultiCC's whole purpose is spawning other programs: git, node, and the
       agent CLIs. Without this, every spawn inherits a restricted environment. -->
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
